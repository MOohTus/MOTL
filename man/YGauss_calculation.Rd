% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MOTL_functions.R
\name{YGauss_calculation}
\alias{YGauss_calculation}
\title{Initialize or update pseudo Y values (YGauss)}
\usage{
YGauss_calculation(
  view,
  likelihoods,
  YTrg,
  Zeta,
  Tau,
  CenterTrg,
  PoisRateCstnt
)
}
\arguments{
\item{view}{a character of current view name data}

\item{likelihoods}{a named list of data types. The list can contain
\code{gaussian}, \code{poisson} or \code{bernoulli} depending of the data
type. Names are the view names.}

\item{YTrg}{current data matrix}

\item{Zeta}{list of Zeta matrices}

\item{Tau}{list of Tau matrices}

\item{CenterTrg}{if FALSE, use the estimated feature weight intercept
from the \code{EstimatedIntercepts.rds} file. If TRUE, use the feature
weight means.}

\item{PoisRateCstnt}{small constant added when transforming Poisson data
to avoid errors}
}
\value{
pseudo Y values for the current view
}
\description{
For gaussian data, Y values (observed data) are centered
(if \code{CenterTrg = TRUE}) and will not change.
For non gaussian, Y values are transformed and change after each
update of Z matrix. The Y pseudo values are centered at each step if
\code{CenterTrg = TRUE}.
For gaussian data this is done for each iteration \code{It>=0}, for others
it is done for each iteration, exept the first one \code{It>0}.
}
\examples{

data("TL_param", package = "MOTL")

view <- "mRNA"
likelihoods <- c("mRNA" = "gaussian", "miRNA" = "gaussian",
                "DNAme" = "gaussian", "SNV" = "bernoulli")
CenterTrg <- FALSE
YTrg <- TL_param$YTrg
Zeta <- TL_param$Zeta
PoisRateCstnt <- 0.0001

YGauss <- YGauss_calculation(view = view,
                               likelihoods = likelihoods,
                               YTrg, Zeta, Tau, CenterTrg, PoisRateCstnt)

}
